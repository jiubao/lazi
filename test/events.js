// var assert = require('assert')
// var eventify = require('../src/events')
import assert from 'assert'

describe('events', function () {
  beforeEach(function () {

  });

  afterEach(function () {})

  describe('#on', function () {
    it ('should be listening on event', function () {
      assert.equal(1, 2/2)
    })
  })
})
